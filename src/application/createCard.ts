import {
    TCard,
    TCardDate,
    TCardStatus
} from '../domain/card';

import TClient from '../domain/client';

const createCard = async (requestCard:any,saveCard:any,carData:any)=>{
    console.log('mock');
}

export default createCard;